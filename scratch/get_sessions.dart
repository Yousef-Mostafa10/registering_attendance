import 'dart:convert';
import 'package:http/http.dart' as http;
import 'package:shared_preferences/shared_preferences.dart';

void main() async {
  // Try to find the latest active session across courses
  print('Not possible to run cleanly via command line since SharedPreferences needs Flutter bindings.');
}
